#!/usr/bin/env ruby
# mode: ruby-mode

# frozen_string_literal: true

require_relative "../lib/fat_term"

FatTerm::Terminal.new(prompt: FatTerm::Prompt.new { "fat_term:#{Dir.pwd}> " }).go(debug: true)
